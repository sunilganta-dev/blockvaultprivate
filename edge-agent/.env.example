# Axis camera on your local network
CAM_URL=http://<CAMERA_IP>/mjpg/video.mjpg?fps=25
CAM_USER=root
CAM_PASS=<CAMERA_PASSWORD>

# VM WebSocket endpoint (edge agent pushes frames here)
VM_WS=ws://<VM_IP>:5700

# Unique ID for this camera
CAM_ID=axis-cam-1
